#!/usr/bin/env bash

MW=$1 node . &
pid=$!

sleep 2

wrk 'http://localhost:3333/?foo[bar]=baz&obj={"a":1}' \
  -d 3 \
  -c 50 \
  -t 8 \
  | grep 'Requests/sec' \
  | awk '{ print "Noapi: " $2 " requests/sec"}'

kill $pid
